/** @type {import('tailwindcss').Config} */
const config = {
  // Minimal config for Tailwind CSS v4 compatibility
  // Most configuration is now done via CSS variables in globals.css
};

export default config;